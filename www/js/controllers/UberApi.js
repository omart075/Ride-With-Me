// UberCtrl that handles API calss

// authorization

angular.module('app').controller('UberCtrl', [$scope, $http, function ($scope, $http) {
    
    var test = function(startLat, startLng, finLat, finLng) = {

    };
}]);
